

DEF f1(x, y) {

    LET y = 10 + 2;
    IF(y == x) {
        y = y + 1;
    } ELIF(y > x) {
        y = y - 1;
    } ELSE {
        y = 0;
    }
    WHILE(y > x) {
        y = y - x;
    }

    RETURN x;
}


DEF main() {

    a = 1;
    a[0] = a[0];
    CALL x = f1(1,1);

    RETURN 0;
}

